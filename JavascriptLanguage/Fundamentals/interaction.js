// Alert a message
alert("Hi, we need your age")

// Prompt
age = prompt("How old are you?", 21)
console.log(age)

// Confirm
isValid = confirm("Are you sure that your age is " + age + "?")
console.log(isValid)